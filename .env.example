# Copy this file to .env. More variables get added here as each part of the app needs them.

# --- Backend ---
NODE_ENV=development
PORT=8000

# Origin allowed to call the API in local (non-docker) dev, where the Vite dev server
# and the API run as separate origins.
CORS_ORIGIN=http://localhost:5173
